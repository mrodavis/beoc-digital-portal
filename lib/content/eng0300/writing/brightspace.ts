/**
 * Where Brightspace lives, if we know.
 *
 * Deliberately empty. The ENG0300 Brightspace shell URL is not recorded
 * anywhere in this repository, and guessing one would send students to a page
 * they cannot submit from — worse than offering no link at all.
 *
 * To turn every "Back to Brightspace" affordance into a working link, paste
 * the course home URL here. Nothing else needs to change.
 */
export const brightspaceCourseUrl = "";

/** Section this site serves, for the URL artifact and page footers. */
export const brightspaceSection = "ENG0300 · Fall 1 2026 · Section 86089";
